#lang setup/infotab

(define scribblings '(("manual.scrbl" ())))
(define name "ssl-url")
(define blurb '(p "Functions mirroring those in net/url, but which work with SSL."))
(define primary-file "ssl-url.rkt")
